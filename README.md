# vue-tsx-counter
Experimental project. Using TypeScriptX(.tsx) with Vue

## Requirements
- vue cli >= 3.0
- node >= 10.10 (tested on 10.15)

## Run
Install dependencies:
`npm install`

Run application:
`npm run serve`
